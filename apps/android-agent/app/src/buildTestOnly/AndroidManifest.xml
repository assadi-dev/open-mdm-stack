<?xml version="1.0" encoding="utf-8"?>
<!--
  buildTestOnly overlay: marks the build as testOnly so a provisioned Device
  Owner can be removed with `adb shell dpm remove-active-admin ...`
  (impossible for a non-test owner). Never applied to debug or release.
  Install with:
      adb install -t -r app-buildTestOnly.apk
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:testOnly="true"
        tools:ignore="TestOnly" />

</manifest>
