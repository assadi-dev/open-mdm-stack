%% Open MDM — per-device ACL. ${clientid} is the device UUID, bound to the
%% device JWT subject by the API's HTTP authenticator.

{allow, all, subscribe, ["mdm/devices/${clientid}/commands"]}.

{allow, all, publish, ["mdm/devices/${clientid}/acks", "mdm/devices/${clientid}/status", "mdm/devices/${clientid}/screen"]}.

{deny, all}.
